<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:theme="@style/AppTheme"
    tools:context=".Activitys.MainActivity">

    <ImageView
        android:id="@+id/logo_menu"
        android:layout_width="wrap_content"
        android:layout_height="100dp"
        android:layout_centerHorizontal="true"
        android:src="@drawable/logolazer"></ImageView>

    <Button
        android:id="@+id/choose_room_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/logo_menu"
        android:layout_centerHorizontal="true"
        android:layout_margin="10dp"
        android:text="Choose Room"></Button>

    <Button
        android:id="@+id/log_out_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/choose_room_btn"
        android:layout_centerHorizontal="true"
        android:layout_margin="10dp"
        android:text="Log Out"></Button>
</RelativeLayout>